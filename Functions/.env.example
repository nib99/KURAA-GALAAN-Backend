# Example env variables (do NOT commit secrets)
FIREBASE_PROJECT_ID=kuraa-galaan-prod
NODE_ENV=production
ADMIN_UIDS=uid1,uid2
# Add email/smtp credentials as GitHub Secrets and access them via process.env in functions
